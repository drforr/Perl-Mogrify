## name transform
## parms {}
## failures 0
## cut
$x = $y > 1 ? 0 : 1;
$x = $y > 1 ? 0 : 1 if 1;
$x = $y > 1 ? 0 : 1 and 1;
1 if $x = $y > 1 ? 0 : 1;
1 and $x = $y > 1 ? 0 : 1;
#-->
$x = $y > 1 ?? 0 !! 1;
$x = $y > 1 ?? 0 !! 1 if 1;
$x = $y > 1 ?? 0 !! 1 and 1;
1 if $x = $y > 1 ?? 0 !! 1;
1 and $x = $y > 1 ?? 0 !! 1;
